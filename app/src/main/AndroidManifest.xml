<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ossd_project">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Placement App"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Each_Company_Details"></activity>
        <activity android:name=".student_eligible_companies" />
        <activity
            android:name=".addcompany"
            android:theme="@style/Theme.AppCompat.Light" />
        <activity android:name=".authcheck">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Editprofile"
            android:label="@string/title_activity_editprofile" />
        <activity
            android:name=".admin_companieslist"
            android:theme="@style/Theme.AppCompat.Light" />
        <activity android:name=".Profile_activity" />
        <activity
            android:name=".Loginactivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity
            android:name=".Registeractivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity android:name=".MainActivity" />

        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="false" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <uses-library
            android:name="com.google.android.wearable"
            android:required="false" />
    </application>

    <uses-permission android:name="android.permission.WAKE_LOCK" />

</manifest>